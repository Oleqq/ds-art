# Обновление от 24.04.2026, final-deploy-audit

- [x] Добавлен финальный аудит готовности и сравнение вариантов деплоя: `docs/implementation/FINAL_AUDIT_AND_DEPLOY.md`.
- [x] Обновлена FTP-инструкция: зафиксировано, что GitHub Actions по FTP может заливать файлы, но без SSH не решает миграции, `storage:link`, cache и rollback БД.
- [x] В FTP-инструкцию добавлен прямой ответ для первого test deploy: одна только заливка файлов по FTP не обновляет MySQL-схему и не создает нужные столбцы.
- [x] FTP-доступы вынесены в локальный секретный файл `docs/private/DEPLOY_ACCESS.local.md`, а не в versioned docs.
- [x] В `docs/implementation/README.md` актуализирован текущий прототип `staff-cabinet (10).html` и тестовые доступы.
- [x] В `phase-07-operations.md` уточнено: `php artisan optimize` выполнять на сервере только при наличии SSH/terminal; без SSH не переносить локальный cache.
- [ ] Перед реальным deploy остаются ручной browser pass admin/employee, проверка production `.env`, SQL import, `public/storage`, прав на `storage` и `bootstrap/cache`.

# Обновление от 23.04.2026, phase 06 employee-kb-parity-pass

- [x] Для employee добавлены реальные маршруты knowledge base на создание, редактирование, загрузку asset-файлов и удаление статьи, если это разрешено персональными правами.
- [x] Экран статьи для employee переведен на тот же editor shell, что и у admin: теперь различие идет по permission flags, а не по отдельному read-only экрану роли.
- [x] Экран категории для employee начал уважать право `Добавлять`: при разрешении появляется создание новой статьи прямо из раздела.
- [x] Поиск и права просмотра дополнительно синхронизированы с авторскими статьями сотрудника: только что созданная сотрудником статья больше не исчезает из его собственного flow.
- [x] В `docs/implementation/README.md` обновлены фактические тестовые доступы Анны Волковой из локальной базы на 23.04.2026.

# Обновление от 23.04.2026, phase 06 access-toggle-sync-fix

- [x] Исправлен экран `/admin/access`: старый конфликт стилей больше не ломает переключатели, они снова кликабельны и визуально ближе к прототипу.
- [x] Исправлен рассинхрон между UI и backend по персональным доступам: если сотруднику явно отключили раздел или статью, экран прав теперь показывает именно это состояние, а не fallback от дефолтной видимости.
- [x] Добавлен regression-test на сценарий с явно отключенными категорией и статьей у сотрудника.
- [x] Проверки после фикса пройдены: `php artisan test --filter=KnowledgeBasePagesTest --stop-on-failure`, `npm run types:check`, `npm run build` через Herd Node `v25.9.0`.

# Обновление от 23.04.2026, phase 06 theme-access-ui-pass

- [x] Новый темный прототип принят как основной визуальный ориентир для экранов приложения.
- [x] Убрано принудительное включение светлой темы; темная тема теперь включается по умолчанию, если пользователь еще не выбирал тему.
- [x] Переключатель темы перенесен в нижний блок sidebar и стилизован ближе к прототипу.
- [x] Экран прав доступа перестроен в формате прототипа: слева список сотрудников, справа права выбранного сотрудника.
- [x] В демо-данных временно оставлены только Михаил Соколов как админ и Анна Волкова как сотрудник.
- [x] Активные пункты sidebar, карточки, preloader и основные темные цвета приведены ближе к красному акценту нового прототипа.
- [x] Проверки пройдены: `php artisan test --filter=KnowledgeBasePagesTest --stop-on-failure`, `npm run types:check`, `vendor/bin/pint --test`, `npm run build`.
- [ ] После следующих скриншотов пройти точечно все оставшиеся UI-расхождения по экранам.

# Статус реализации

## Текущий ориентир

- цель MVP: функциональный и визуальный паритет с `docs/prototype/staff-cabinet (3).html`
- рабочий репозиторий: `C:\dev\ds-art`
- текущая стадия: `Этап 07. Эксплуатационная готовность`

## Этапы

- [x] Этап 01. Основа проекта
- [x] Этап 02. Аутентификация, роли, layouts
- [x] Этап 03. Модуль сотрудников
- [x] Этап 04. Структура базы знаний
- [ ] Этап 05. Статьи и редактор
- [ ] Этап 06. Права доступа, поиск, стабилизация
- [ ] Этап 07. Эксплуатационная готовность
- [ ] Этап 08. AI-агент — сознательно отложен до конца

## Что уже собрано

- [x] Laravel + Inertia + React + TypeScript работают в основном репозитории
- [x] MySQL подключен, миграции и сиды рабочие
- [x] локальный `MySQL` может требовать ручного старта после перезапуска машины через `scripts/start-local-mysql.ps1`
- [x] auth, роли `admin/employee`, разделение `/admin` и `/employee`
- [x] demo-пользователи и demo-сотрудники заведены в БД
- [x] светлая тема зафиксирована как базовая для MVP
- [x] SPA-поведение закреплено как правило для всего приложения через корректный Inertia flow
- [x] для незавершенных фич есть planned-feature tooltip

## Этап 04. Структура базы знаний

- [x] доменная модель категорий и статей
- [x] MySQL-миграции для базы знаний
- [x] demo-дерево из HTML-прототипа
- [x] главная страница БЗ для `admin`
- [x] главная страница БЗ для `employee`
- [x] экран категории для `admin`
- [x] экран категории для `employee`
- [x] реальные данные дерева в sidebar
- [x] admin CRUD для разделов
- [x] picker иконок разделов
- [x] hover-controls и контекстное меню разделов в дереве
- [x] create/edit flow разделов прямо из дерева
- [x] кастомный confirm popup вместо нативного browser confirm
- [x] файловая загрузка обложки раздела вместо URL-поля
- [x] поддержка своей иконки-файла для раздела
- [x] быстрые KB-мутаторы без hard refresh через корректный `return_to + router.reload()`
- [x] KB home и shell подтянуты ближе к прототипу по выравниванию, карточкам и отступам
- [x] mobile sidebar/drawer закрывается корректно при переходах по разделам БЗ
- [x] mobile bottom-nav и mobile header усилены под более нативный UX-поток

## Этап 05. Статьи и редактор

### Уже сделано

- [x] добавлены маршруты статей для `admin` и `employee`
- [x] добавлен backend flow создания статьи из раздела
- [x] добавлена живая Inertia-страница статьи для `admin`
- [x] добавлена read-only страница статьи для `employee`
- [x] статья открывается из раздела без hard refresh
- [x] статья сохраняется через Inertia `PUT` без `F5`
- [x] поддержана загрузка обложки статьи
- [x] поддержана иконка статьи через picker
- [x] поддержано базовое описание и текст статьи
- [x] добавлено удаление статьи через кастомный confirm popup
- [x] добавлены feature-тесты для article flow
- [x] добавлен block-editor со структурным хранением `blocks` в БД
- [x] добавлены действия `дублировать`, `переместить`, `копировать ссылку` в toolbar статьи
- [x] добавлена загрузка asset-блоков статьи: изображение, файл, видео
- [x] employee-экран статьи переведен на тот же editor shell, что и у admin, с переключением доступных действий по permission flags
- [x] добавлено оглавление статьи по заголовкам `h2/h3`

### Что пока еще не закрыто

- [ ] richer inline-formatting внутри текстовых блоков
- [x] права доступа на статью
- [ ] полное 1:1 поведение article-toolbar и контекстных меню из прототипа
- [ ] точный визуальный паритет article-screen с HTML

## Последние инженерные решения

- [x] для категорий оставлено файловое хранение обложки на `public` disk, а не URL-поле
- [x] для иконки раздела добавлен смешанный режим: библиотека иконок или своя картинка
- [x] create/update/delete сценарии KB удерживаются на Inertia без кастомной SPA-обертки
- [x] confirm-сценарии переведены на собственный popup-слой
- [x] article-flow начат с реального открытия и сохранения статьи, а не с имитации редактора
- [x] article editor хранит контент как структурные блоки, а не как один textarea blob
- [x] duplicate/move/assets для статьи закрыты backend-тестами

## Ближайший следующий шаг

- [ ] довести экран статьи ближе к прототипу по стилям, spacing и toolbar
- [ ] добрать inline-formatting, более богатый table UX и дополнительные block actions
- [ ] после этого готовить переход к `Этапу 06` с доступами к статье

## Обновление по редактору от 20.04.2026

- [x] экран статьи перестроен ближе к прототипу: breadcrumb, обложка, иконка, крупный заголовок и разделенный toolbar
- [x] старый карточный editor-flow заменен на линейный блоковый поток без громоздких оболочек
- [x] у блоков появились прототипные левые контролы: `+` для вставки и меню действий блока
- [x] добавлены контекстные действия блока: конвертация, перемещение вверх/вниз, удаление
- [x] кодовый блок переведен на темный вид с кнопкой копирования, ближе к прототипу
- [x] блоки изображения, файла, видео, таблицы и ссылки перестроены под более близкий к прототипу UI
- [x] типизация, knowledge base тесты и production build после перестройки редактора проходят

## Обновление по редактору от 21.04.2026

- [x] текстовые блоки статьи переведены ближе к прототипу по способу редактирования: теперь это более живой contenteditable-flow, а не ощущение формы из textarea-карточек
- [x] размеры и позиционирование левых контролов блока поджаты ближе к прототипу: `+` и handle теперь живут в более компактной зоне
- [x] типографика блоков `p / h2 / h3 / quote / list` подтянута ближе к HTML-прототипу
- [x] у contenteditable-блоков добавлены прототипные placeholder-состояния
- [x] mobile-отступы и размеры блоков редактора дополнительно подправлены под более нативный UX

## Обновление по редактору от 21.04.2026, rich text

- [x] текстовые блоки статьи теперь сохраняют не только plain text, но и `html`, чтобы inline-форматирование не терялось после сохранения статьи
- [x] добавлен floating inline-toolbar ближе к прототипу: жирный, курсив, подчеркивание, зачеркивание и ссылка
- [x] rich text из редактора теперь корректно рендерится и в read-only режиме статьи
- [x] после добавления rich text слоя `types:check`, KB feature tests и production build проходят

## Опорный документ по паритету с прототипом

- [x] полный аудит прототипа: `docs/implementation/PROTOTYPE_FULL_AUDIT.md`

## Обновление от 21.04.2026, phase 05

- [x] редактор статьи стал ближе к прототипу по flow ввода: `Enter` в текстовом блоке создает следующий абзац
- [x] таблицы в редакторе переведены на contenteditable-ячейки и получили `Tab`-навигацию
- [x] после добавления строки/столбца фокус переходит в новую ячейку без hard refresh
- [x] списковые блоки переведены из textarea в более живой contenteditable-flow
- [ ] в этапе 05 еще остаются мелкий spacing/hovers и финальный визуальный 1:1 проход по article-screen

## Обновление от 21.04.2026, phase 05 visual

- [x] article-screen стал ближе к прототипу по toolbar, иконкам и отступам
- [x] кнопки действий статьи приведены к более прототипному виду
- [x] cover / icon controls и ритм editor-screen дополнительно поджаты к макету
- [ ] в phase 05 еще остается финальный проход по мелким spacing / hovers / popup-меню редактора

## Обновление от 21.04.2026, phase 05 visual pass 2

- [x] экран статьи дополнительно поджат по ширине и вертикальному ритму ближе к HTML-прототипу
- [x] сценарий иконки статьи приведен ближе к прототипу: если иконка уже есть, показывается крупный glyph; если иконки нет, показывается pill `Добавить иконку`
- [x] toolbar статьи приведен ближе к макету по посадке кнопок, эмодзи-иконкам и расстояниям между группами
- [x] popup выбора типа блока и меню действий блока поджаты по ширине, скруглениям и тени ближе к прототипу
- [x] knowledge base тесты, `types:check` и production build после визуального прохода снова зеленые
- [ ] в `Этапе 05` еще остается финальный проход по micro-hover состояниям и мелким расхождениям editor popup-слоя

## Обновление от 21.04.2026, база и phase 05 pass 3

- [x] локальный `MySQL` снова поднят, база `ds_art` отвечает, `php artisan tinker` подтверждает рабочее подключение
- [x] `ds-art.test` снова отвечает `200 OK` на `/login`, после старой error-страницы в браузере нужен жесткий `Ctrl + F5`
- [x] скрипт [start-local-mysql.ps1](C:/dev/ds-art/scripts/start-local-mysql.ps1) усилен: время ожидания старта увеличено, чтобы локальный `mysqld` не считался упавшим слишком рано
- [x] в редакторе статьи левые контролы блока возвращены ближе к прототипу: `+` и `⠿` вместо более "системных" иконок
- [x] footer-кнопка `＋ Добавить блок` и popup-меню редактора дополнительно приближены к прототипу по иконкам и посадке
- [ ] по `Этапу 05` еще остался последний визуальный polish popup/hovers и финальная подгонка editor spacing

## Обновление от 21.04.2026, phase 05 popup-pass 4

- [x] popup-слой редактора переведен на один активный fixed-menu за раз: меню добавления блока и меню действий блока больше не наслаиваются
- [x] для popup-меню добавлено закрытие по клику вне меню и по `Escape`, ближе к поведению HTML-прототипа
- [x] позиционирование popup-меню отвязано от потока блока и приближено к `bpicker / bctx`-слою из прототипа
- [x] кнопки `+` / `⠿`, внутренние отступы, ширина и тень popup-меню дополнительно поджаты к прототипу
- [x] после popup-pass `types:check`, KB feature tests и production build снова проходят
- [ ] в phase 05 еще остается последний визуальный проход по мелким hover/spacing-расхождениям article-editor

## Обновление от 21.04.2026, phase 05 editor-pass 6
- Добавлено оглавление на editor-экране статьи: headings h2/h3 собираются из блоков и выводятся в sticky sidebar справа, ближе к прототипу.
- Экран статьи переведен на editor-layout с правым TOC и более точной посадкой заголовка: если у статьи есть иконка, title/meta теперь стоят правее крупной иконки, как в макете.
- В block editor добавлен drag-and-drop reorder через handle ⠿ без hard refresh; видны drop-indicator линии до/после блока.
- Меню действий блока переработано: Конвертировать в вынесено в отдельный submenu, popup-слой стал аккуратнее и ближе к прототипу по поведению и отступам.
- Проверки после pass: 
pm run types:check, rtisan test --filter=KnowledgeBase --stop-on-failure, production build через Herd Node — все проходят.


## Обновление от 22.04.2026, phase 05 editor-pass 7
- Из article editor убрана лишняя подпись типа блока под контентом: она не соответствует прототипу и только засоряла экран.
- Подтянуты отступы editor-screen: добавлен внутренний padding страницы статьи, breadcrumbs и контент больше не липнут к sidebar.
- Editor-layout шире и ближе к прототипу: shell и правый TOC разведены по сетке, а sticky TOC теперь нормально встает в правую колонку при наличии заголовков h2/h3.
- Проверки после pass: 
pm run types:check, rtisan test --filter=KnowledgeBase --stop-on-failure, production build через Herd Node — проходят.


## Обновление от 22.04.2026, phase 05 editor-pass 8
- Оглавление на editor-экране сделано умнее: если в статье есть h2/h3, строится обычный TOC по ним; если заголовков пока нет, TOC собирается из первых содержательных блоков статьи (текст, файл, видео, ссылка и т.д.).
- Anchors переведены на корневые блоки редактора, поэтому прокрутка от TOC стала стабильнее и не зависит от внутреннего contenteditable узла.
- Popup выбора типа блока подтянут ближе к прототипу: меню уже, плотнее по паддингам, с более корректным ритмом и submenu для действий блока.
- Проверки после pass: 
pm run types:check, rtisan test --filter=KnowledgeBase --stop-on-failure, production build через Herd Node — проходят.


## Обновление от 22.04.2026, phase 05 editor-pass 9
- Убраны fallback-иконки для статей и разделов: теперь элемент может быть реально без иконки.
- В picker иконок добавлен явный вариант `Без иконки`, чтобы иконку можно было не только заменить, но и снять.
- Подтянуты отступы editor-screen, breadcrumb и article-layout ближе к прототипу.
- Popup выбора блока и submenu конвертации поджаты по ширине, внутренним отступам и позиции относительно блока.
- Popup действий блока и submenu конвертации переведены на portal-рендер в `document.body`, чтобы меню не уезжало по координатам внутри editor-layout.
- Кнопка `⠿` в editor controls больше не работает как draggable-handle и теперь стабильно прожимается как меню действий блока.
- TOC расширен и получил перенос длинных слов/цифр, чтобы текст не вылезал за границы карточки.
- В media-блоках редактора добавлены явные действия очистки и удаления блока для изображения, файла и видео.
- Видео-блок теперь показывает preview после загрузки, чтобы URL не выглядел как подвисшее состояние.

## Обновление от 22.04.2026, phase 05 editor-pass 10
- Переработан control `⠿` у блока статьи: короткий клик открывает меню конвертации/перемещения/удаления, а drag включается только после движения мышью. Это убирает конфликт, из-за которого меню могло не прожиматься.
- Возвращена drag-and-drop перестановка блоков без native `draggable`: drop-indicator остается внутри редактора и не ломает клик по меню блока.
- Из media-блоков убраны технические inline-кнопки очистки/удаления. Теперь файл, изображение или видео сначала редактируются, затем фиксируются через `Готово`; удалить блок можно только через общее меню блока.
- Для завершенных media-блоков добавлен безопасный режим просмотра с кнопкой `Редактировать`, чтобы контент нельзя было случайно стереть в процессе обычного редактирования статьи.
- Проверки после прохода: `npm run types:check`, `php artisan test --filter=KnowledgeBase --stop-on-failure`, production build Vite через Herd Node — проходят.

## Обновление от 22.04.2026, phase 05 editor-pass 11
- Экран статьи разделен на два режима: при входе открывается просмотр, а основная кнопка сверху показывает `Редактировать`.
- После клика `Редактировать` включается режим редактирования статьи; та же кнопка становится `Сохранить`, а после успешного сохранения экран возвращается в просмотр.
- Сложные блоки статьи теперь не открывают поля редактирования сразу. Таблицы, файлы, видео, изображения, списки, код и ссылки сначала выглядят как контент; в режиме редактирования у них появляется hover-кнопка `Редактировать`.
- Заголовок, иконка и обложка статьи в режиме просмотра стали read-only, чтобы статья при входе не ощущалась как открытая форма.
- Проверки после прохода: `npm run types:check`, `php artisan test --filter=KnowledgeBase --stop-on-failure`, production build Vite через Herd Node — проходят.

## Обновление от 22.04.2026, phase 05 encoding-fix
- Исправлена битая UTF-8 строка в кнопке `Редактировать` у сложных блоков редактора статьи.
- Проверен `resources/js` на похожие mojibake-строки: новых в исходниках не найдено.
- Пересобран production build Vite, чтобы `public/build` больше не отдавал старый битый текст.
- Проверки после фикса: `npm run types:check`, `php artisan test --filter=KnowledgeBase --stop-on-failure`, production build Vite через Herd Node — проходят.

## Обновление от 22.04.2026, phase 05 editor-cursor-fix
- Для control `⠿` у блоков статьи добавлен cursor `grab`, а во время нажатия и активного drag — `grabbing`.
- Добавлены `touch-action: none` и `user-select: none`, чтобы handle вел себя стабильнее при drag-and-drop и не выделял текст.

## Обновление от 22.04.2026, phase 05 database-structure-final
- Зафиксирована структура хранения статей перед переходом к этапу 6: `knowledge_articles` остается главной таблицей статьи, а `blocks` остается основным JSON-форматом редактора.
- Добавлены таблицы `knowledge_article_blocks` и `knowledge_article_assets`, чтобы каждый блок статьи и каждый файл/картинка/видео были видны в SQL и Adminer отдельно.
- Добавлен backend-синхронизатор `KnowledgeArticleStructureSync`: он обновляет SQL-слой блоков/ассетов при создании, сохранении и дублировании статьи.
- Добавлена artisan-команда `php artisan knowledge-base:sync-article-structure` для ручной пересинхронизации существующих статей.
- Локальная MySQL-БД обновлена миграциями и синхронизирована: сейчас в ней `20` статей, `42` блока статей, `1` asset-запись, `15` разделов.
- Добавлена документация: `docs/implementation/areas/database-content-storage.md`.
- Проверки после изменения: `pint --test`, `npm run types:check`, `php artisan test --filter=KnowledgeBase --stop-on-failure` проходят.

## Обновление от 22.04.2026, phase 05 toc-jump-fix
- Исправлен переход по оглавлению в длинных статьях: клик теперь явно скроллит к нужному блоку через `scrollTo`, а не полагается только на нативный hash-jump браузера.
- Такой же переход добавлен для read-only renderer, чтобы employee-страницы статей вели себя одинаково.
- Для целевых блоков добавлен `scroll-margin-top`, чтобы после прыжка блок не прилипал к верхней границе окна.

## Обновление от 22.04.2026, phase 06 app-preloader
- Добавлен глобальный компонент `AppPreloader`, который слушает Inertia `start / finish / cancel` и показывает загрузку при переходах между разделами приложения.
- Preloader сделан в стиле Staff Cabinet: тонкая зеленая progress-линия сверху, компактный floating badge `SC`, текст `Загружаем раздел` и анимированные точки.
- Компонент не блокирует клики и появляется только если переход длится дольше короткой задержки, чтобы быстрый SPA-flow не мигал лишними анимациями.
- Добавлена поддержка светлой и темной темы.
- Проверки после изменения: `npm run types:check`, production build Vite через Herd Node — проходят.
# Обновление от 22.04.2026, phase 06 search-start

- Локальная MySQL-БД поднята через `scripts/start-local-mysql.ps1`; `mysqld` слушает `127.0.0.1:3306`.
- Добавлен backend-поиск по базе знаний: поиск идет по разделам, статьям, `knowledge_article_blocks.plain_text/html` и `knowledge_article_assets`.
- Поиск разделен по ролям: admin видит черновики и скрытые материалы, employee видит только опубликованные статьи из видимых разделов.
- В sidebar вместо planned-tooltip теперь стоит рабочий поиск, который ведет на Inertia-страницы `/admin/knowledge-base/search` и `/employee/knowledge-base/search`.
- Проверки после изменения: `vendor/bin/pint --test`, `npm run types:check`, `php artisan test --filter=KnowledgeBase --stop-on-failure` проходят.
- Production build проходит через Herd Node `v25.9.0`; системный Node `18.16.0` для текущего Vite уже не подходит.

## Обновление от 23.04.2026, phase 06 sidebar-resize

- [x] убрано горизонтальное переполнение у desktop-sidebar: контейнеры и дерево БЗ теперь принудительно живут в `min-width: 0 / overflow-x: hidden`
- [x] добавлен desktop-resize сайдбара вправо с сохранением ширины в cookie
- [x] offcanvas-логика сайдбара теперь опирается на текущую ширину, а не на старый фиксированный preset
- [x] preview-поиск в sidebar сделан адаптивным к новой ширине панели

## Обновление от 23.04.2026, phase 06 sidebar-profile-and-search-preview

- [x] preview-поиск в sidebar теперь строго равен ширине input и не расширяется за пределы поля
- [x] нижний user-блок в sidebar стал ссылкой на `/settings/profile` для admin и employee
- [x] если у пользователя есть фото сотрудника, оно передается в `auth.user.avatar` и показывается как аватар
- [x] если фото нет, показываются инициалы имени/фамилии в стабильном цветном круге

## Обновление от 23.04.2026, phase 06 access-rights-pass

- [x] Добавлен уровень доступа статьи `access_level`: `inherit`, `employees`, `admins`, `author`.
- [x] Доступ к статьям теперь проверяется на backend-уровне через `KnowledgeArticlePolicy`, а не только через скрытие UI.
- [x] Employee больше не может открыть или найти admin-only статью через поиск.
- [x] Добавлен экран `/admin/access` для управления видимостью разделов, публикацией статей и уровнем доступа статей.
- [x] Форма создания статьи получила выбор уровня доступа.
- [x] Мобильная навигация переведена в нижнее меню: команда/база/доступ/поиск/меню для админа и профиль/база/поиск/меню для сотрудника.
- [x] На мобильных устройствах поиск из sidebar ведет на полноценный экран поиска, а не открывает маленький popup.
- [x] Экран профиля перестилизован под Staff Cabinet и переведен на русский.
- [x] Локальная MySQL обновлена миграцией `2026_04_23_010000_add_access_level_to_knowledge_articles_table.php`.
- [x] Проверки прошли: `KnowledgeBasePagesTest`, `npm run types:check`, `pint --test`, production build Vite.
- [x] Следующий подпроход этапа 6: русский UI безопасности/2FA, audit log критичных действий.
- [ ] Осталось вручную проверить admin/employee-flow в браузере и мобильную навигацию.

## Обновление от 23.04.2026, phase 06 security-audit-pass

- [x] Экран безопасности `/settings/security` переписан чистым русским текстом и приведен к `settings-card/settings-form` стилю.
- [x] 2FA modal и recovery-codes переведены на русский.
- [x] Flash-сообщения профиля и смены пароля переведены на русский.
- [x] Добавлены `audit_logs`, модель `AuditLog` и сервис `AuditLogger`.
- [x] В аудит пишутся смена доступа разделов/статей, дублирование, перемещение и удаление статей, удаление разделов.
- [x] Документ этапа 6 обновлен с актуальными `[x] / [ ]` чекбоксами.
- [x] Проверки прошли: `npm run types:check`, `php artisan test --filter=KnowledgeBasePagesTest --stop-on-failure`, `php artisan test --filter=Settings --stop-on-failure`, `php artisan test --filter=Profile --stop-on-failure`, `pint --test`, production build Vite через Herd Node.

## Обновление от 23.04.2026, phase 07 deploy-docs

- [x] Добавлена инструкция по заливке и подъему приложения через FTP/phpMyAdmin: `docs/implementation/DEPLOY_FTP.md`.
- [x] Переписан этап 7 как эксплуатационный чек-лист: бэкапы, storage, production `.env`, deploy, Redis/очереди без лишнего подключения.
- [x] Добавлен шаблон `.env.production.example`.
- [x] AI-этап 8 зафиксирован как отложенный до конца.
- [ ] Перед реальным deploy остается ручной проход admin/employee-flow, проверка мобильной навигации и решение по `storage` symlink на хостинге.
# Обновление от 23.04.2026, phase 06 prototype-theme-access-pass

- [x] Новый прототип `docs/prototype/staff-cabinet (10).html` принят как актуальный ориентир по цветам светлой/темной темы и экрану прав доступа.
- [x] Убрано принудительное включение светлой темы: приложение теперь корректно применяет сохраненную тему, а по умолчанию ближе к темному прототипу.
- [x] Переключатель темы перенесен в нижний блок sidebar рядом с профилем, как в прототипе.
- [x] Экран прав доступа приведен к employee-centric логике прототипа: сотрудник слева, права действий и доступ к разделам/статьям справа.
- [x] В демо-данных временно оставлены только Михаил Соколов как админ и Анна Волкова как сотрудник.
- [x] Sidebar, активные состояния, быстрый поиск и preloader переведены на красный accent-подход прототипа.
- [ ] Остался ручной визуальный проход по экранам после сборки: сотрудники, база знаний, категория, статья, права доступа.
